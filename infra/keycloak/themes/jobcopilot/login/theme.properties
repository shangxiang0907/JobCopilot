# JobCopilot login theme — extends the stock keycloak.v2 (PatternFly v5) theme.
# Only overrides login-verify-email.ftl: the stock page is a dead end when the
# verification link is opened on ANOTHER device (the original browser has no
# way forward). See the template for details.
parent=keycloak.v2
